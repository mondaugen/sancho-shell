[alias]
	co = checkout --recurse-submodules
	b = branch
	c = commit
	s = status
	cl = clone --recurse-submodules
	d = diff
	rs = restore --staged
	tree = log --graph --all --decorate --oneline
	difc = diff --color
	dno = diff --name-only
